<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="wrap_content"
    android:layout_marginBottom="8dp" android:background="@drawable/card_unselected"
    android:gravity="center_vertical" android:minHeight="68dp" android:paddingStart="12dp"
    android:paddingTop="8dp" android:paddingEnd="6dp" android:paddingBottom="8dp">
    <com.google.android.material.switchmaterial.SwitchMaterial
        android:id="@+id/subscriptionSwitch" android:layout_width="0dp" android:layout_height="wrap_content"
        android:layout_weight="1" android:buttonTint="@color/dadway_primary"
        android:ellipsize="middle" android:maxLines="2" android:textColor="@color/dadway_text"
        android:textSize="13sp"/>
    <ImageButton android:id="@+id/deleteSubscriptionButton" android:layout_width="44dp"
        android:layout_height="44dp" android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="Удалить подписку" android:padding="11dp"
        android:src="@drawable/ic_delete" android:tint="@color/dadway_danger"/>
</LinearLayout>
